name: Build Android APK (Jornada BRF)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - name: Install dependencies
        run: |
          npm install
          npm install @capacitor/cli @capacitor/android

      - name: Build Web Project (Vite)
        run: |
          # Esto genera la fecha automática para tu app
          export VITE_BUILD_DATE=$(date +'%y%m%d.%H%M')
          npm run build

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build Android APK
        run: |
          npx cap sync android
          cd android
          chmod +x gradlew
          # Usamos Java 21 para compilar sin errores
          ./gradlew assembleDebug --no-daemon -Dorg.gradle.java.home=$JAVA_HOME_21_X64

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: JornadaBRF-App
          path: android/app/build/outputs/apk/debug/*.apk
